server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/parking
    username: parking
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    database: postgresql
    generate-ddl: true

